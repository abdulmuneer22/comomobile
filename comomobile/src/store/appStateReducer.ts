const initialState = {
  config: {
    baseURL: "https://como.gitspark.com",
  },
};

export default (state = initialState, { type }: any) => {
  switch (type) {
    default:
      return state;
  }
};
